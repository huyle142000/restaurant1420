"use client";
import React from "react";
import { useTranslation } from "react-i18next";

type Props = {};

const ExpandItem = (props: Props) => {
  const { t } = useTranslation();
  return <div>ExpandItem</div>;
};

export default ExpandItem;
